@page "/"
@inject NavigationManager Nav
@inject MauiApp1.Data.JournalDb Db

@code {
    protected override async Task OnInitializedAsync()
    {
        if (await Db.HasPinAsync())
            Nav.NavigateTo("/pin", true);
        else
            Nav.NavigateTo("/pin-setup", true);
    }
}
